# The base URL of your backend API (without the /api suffix)
# For local development, this is usually http://localhost:5000
# For production (like Coolify), change this to your backend domain (e.g., https://api.yourdomain.com)
VITE_API_BASE_URL=https://api.aineam.smartagency-ye.com

